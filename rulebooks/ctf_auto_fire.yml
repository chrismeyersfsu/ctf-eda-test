---
- name: CTF Auto Fire Rules
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 5
  rules:
    - name: Fire on first event
      condition: event.i == 1
      action:
        run_playbook:
          name: playbooks/render_vars.yml
